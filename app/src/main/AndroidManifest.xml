<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- READ: No INTERNET permission. This is intentional and non-negotiable. -->

    <!-- Needed for API 26-32 SAF file reading; auto-removed on API 33+ -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- WorkManager foreground service for initial indexing (required API 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- WorkManager reschedule after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- WorkManager keeps CPU awake during indexing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Show the indexing progress notification (runtime permission on Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- INTENTIONALLY ABSENT:
         android.permission.INTERNET
         android.permission.ACCESS_NETWORK_STATE
         android.permission.MANAGE_EXTERNAL_STORAGE
         Any ad/analytics SDK permissions -->

    <!-- ML Kit transitively pulls in com.google.android.datatransport (Google's
         Clearcut telemetry backend), which merges INTERNET + ACCESS_NETWORK_STATE
         back into the manifest. Force-remove them so the shipped app physically
         cannot make network requests. Without INTERNET the transport backend is inert. -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />

    <application
        android:name=".LumenApplication"
        android:largeHeap="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lumen">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lumen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Text-selection toolbar entry ("Search in Lumen"): an alias so the
             PROCESS_TEXT surface carries its own label while the handling stays
             in MainActivity. -->
        <activity-alias
            android:name=".launcher.ProcessTextSearch"
            android:exported="true"
            android:label="@string/process_text_label"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <!-- Home-screen "Search Lumen" pill. Not exported: the system delivers
             APPWIDGET_UPDATE regardless; other apps have no business here. -->
        <receiver
            android:name=".launcher.SearchWidgetProvider"
            android:exported="false"
            android:label="@string/widget_search_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_search_info" />
        </receiver>

        <!-- WorkManager foreground service, must declare type to match FOREGROUND_SERVICE_DATA_SYNC -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Disable default WorkManager initializer so Hilt can provide WorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup.InitializationProvider"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
